_site/
.jekyll-cache/
.jekyll-metadata
.DS_Store

# Textbooks — copyright, never commit or deploy
textbooks/

# Private grade data — never commit
grades/grades.xlsx
grades/grade-data/

# Deployment script (contains server path)
deploy.ps1
